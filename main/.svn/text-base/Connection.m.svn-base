% A directed connection between two Nodes, corresponding roughly to an axon
% or group of axons. 
classdef Connection < handle
    
    properties (Access = public)
        origin = [];
        termination = [];
    end
    
    methods (Access = public)
        
        % origin: The source end of the Projection
        % termination: The destination end 
        function c = Connection(origin, termination)
            c.origin = origin;
            c.termination = termination;
        end        
    end
end